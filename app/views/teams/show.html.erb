<% @title = "#{@team.name}" %>
<p>
  <strong>Name:</strong>
  <%= @team.name %>
</p>

<p>
  <strong>Color:</strong>
  <%= @team.color %>
</p>

<%= link_to 'Edit', edit_board_team_path(@board, @team) %> |
<%= link_to 'Back', board_teams_path(@board) %>