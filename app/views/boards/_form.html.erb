<div class="row">
  <div class="col-md-4">
    <%= bootstrap_form_for(@board, layout: :inline) do |f| %>
      <% if @board.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@board.errors.count, "error") %> prohibited this board from being saved:</h2>

          <ul>
          <% @board.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.text_field :name %>
      </div>
      <div class="form-group">
        <%= f.text_field :group_id_for_yammer_post %>
      </div>
      <%= f.submit %>
    <% end %>
  </div>
</div>